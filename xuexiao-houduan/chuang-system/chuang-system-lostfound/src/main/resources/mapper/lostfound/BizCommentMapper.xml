<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.zhangchuangla.system.lostfound.mapper.BizCommentMapper">

    <!-- 结果映射 -->
    <resultMap id="CommentResultMap" type="cn.zhangchuangla.system.lostfound.model.entity.BizComment">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="userName" column="user_name"/>
        <result property="postTitle" column="post_title"/>
    </resultMap>

    <!-- 分页查询帖子评论（带用户名） -->
    <select id="selectPostCommentsWithUserName" resultMap="CommentResultMap">
        SELECT c.*,
               u.username AS user_name
        FROM biz_comment c
        LEFT JOIN sys_user u ON c.user_id = u.user_id
        WHERE c.post_id = #{postId}
          AND c.status = #{status}
          AND c.is_deleted = '0'
        ORDER BY c.create_time DESC
    </select>

    <!-- 管理端分页查询评论（带用户名和帖子标题） -->
    <select id="selectAdminCommentsWithDetail" resultMap="CommentResultMap">
        SELECT c.*,
               u.username AS user_name,
               p.title AS post_title
        FROM biz_comment c
        LEFT JOIN sys_user u ON c.user_id = u.user_id
        LEFT JOIN biz_post p ON c.post_id = p.id
        WHERE c.is_deleted = '0'
        <if test="status != null">
            AND c.status = #{status}
        </if>
        ORDER BY c.create_time DESC
    </select>

</mapper>
