<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.zhangchuangla.system.lostfound.mapper.BizGiftMapper">

    <!-- 扣减库存 -->
    <update id="decreaseStock">
        UPDATE biz_gift
        SET stock = stock - #{quantity},
            updated_at = NOW()
        WHERE id = #{giftId}
          AND stock >= #{quantity}
          AND deleted = 0
    </update>

    <!-- 增加兑换数量 -->
    <update id="increaseExchangeCount">
        UPDATE biz_gift
        SET exchange_count = exchange_count + #{quantity},
            updated_at = NOW()
        WHERE id = #{giftId}
          AND deleted = 0
    </update>

</mapper>
